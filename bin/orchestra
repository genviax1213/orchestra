#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
VENV_BIN="$ROOT_DIR/.venv/bin/orchestra"

if [[ ! -x "$VENV_BIN" ]]; then
  echo "error: $VENV_BIN not found or not executable" >&2
  echo "run: make install" >&2
  exit 1
fi

exec "$VENV_BIN" "$@"
